import React from 'react';
import {TextField, Button, Typography ,Color} from '@material-ui/core'

import  useStyles  from './styles';

const Form = () => {
    const classes=useStyles();
    return(
        <h1>FORM</h1>
    );
}

export default Form;